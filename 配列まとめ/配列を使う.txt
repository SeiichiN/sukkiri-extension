int[] scores = new int[3];

scores[0] = 20;
scores[1] = 30;
scores[2] = 40;

// これはできない
scores[3] = 50;
// 配列のサイズは初期化時に決定され、変更できない。

for (int i = 0; i < scores.length; i++) {
	System.out.println(scores[i]);
}

// 拡張for文
for (int num : scores) {
  System.out.println(num);
}

